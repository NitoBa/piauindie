query getHighlightCourses {
  courses(where: { isHighlighted: { equals: true } }) {
    id
    title
    description
    slug
    thumbnailUrl
    courseEvaluation
    price
    teacher {
      name
      avatarUrl
    }
  }
}
